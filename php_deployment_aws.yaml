apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    tier: backend
spec:
  template:
    metadata:
      labels:
        app: php
        tier: backend
    spec:
      volumes:
        - name: code
          PersistentVolumeClaim:
            claimName: code
      containers:
        - name: php
          image: php:7-fp
          volumeMounts:
            - name: code
              mountPath: /code
      initContainers:
        - name: install
          image : busybox
          volumeMounts:
            - name: code
              mountPath: /code
          command:
            - wget
            - "-O"
            - "/code/index.php"
            - https://raw.githubusercontent.com/do-community/php-kubernetes/master/index.php
  replicas: 1
  selector:
    matchLabels:
      app: php
      tier: backend
